# 交接文档（双 AI 固定交接面）

> **规则（见 AGENT.md §6）**：实现方每轮改完，在下方"待审计"表新增/更新条目
> （写明项目、提交、改动摘要、审计要点）；审计方在对应条目下追加"审计结论"
> （复验取证 + 新发现编号）；条目闭环后移入"已闭环"表。双 AI **只通过本文件**
> 交接工作状态——有新条目即开始审计，无需其他通知。

## 待审计

（空——H1 已闭环，见下）

## 已闭环

### H1：sender 单测覆盖（V0.2.4→V0.2.5，两轮审计全闭环）

- **V0.2.4（9d77981）**：新增 7 个测试函数（停等 ACK / 重试不丢红线 / 断连保 WAL /
  滑窗按序 ACK / go-back-N 尾窗重发 / 退避）；审阅方审计发现 R12~R14。
- **V0.2.5（47f84a6）复验（审阅方 59c4f03 后追加）**：
  - R12 ✅：ackServer 增 `nackConnPersistent`（nack 后该连接恒 0x00，模拟持续 import
    失败）；审阅方独立复现取证实验——临时移除 `s.client.Close()` → 测试 FAIL
    （8.08s，received=30191 死连接打转不收敛），恢复 → 0.053s 通过；
    `go test -count=3` + race 全绿；
  - R13 ✅：计数更正为 7；R14 ✅：DLQ 过时注释更正为“发送端永不 DLQ，重试超限只告警”。
  - fork 已同步移植（2233e0d）。全量 vm-sync `go test ./...` 9 包全绿。

- **V0.2.0 审计周期**（三轮复审全闭环，见 docs/audit-v020-review-2026-08-16.md）：
  VM1~VM6（六项移植）→ R1~R7（复审缺陷）→ R8（自查缺陷）→ R9~R11（第二轮缺陷），
  审阅方 6327c6d 宣布“审计周期关闭，无未决项”。
